apiVersion: v1
kind: ConfigMap
metadata:
  name: voice-agent-proxy-headers
  namespace: prod
data:
  Upgrade: "$http_upgrade"
  Connection: "upgrade"
  Host: "$host"
  X-Real-IP: "$remote_addr"
  X-Forwarded-For: "$proxy_add_x_forwarded_for"
